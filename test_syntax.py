def test_logical_not():
    # Practicing alternatives to '!'
    is_valid = True
    assert not is_valid == False  # Using 'not' instead of '!'
    assert is_valid is not False  # Using 'is not'

def test_comparison():
    x = 10
    y = 20
    assert x != y  # Standard 'not equal' syntax

